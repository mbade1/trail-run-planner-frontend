How to solve npm error “npm ERR! code ELIFECYCLE”
    1: npm cache clean --force
    2: rm -rf node_modules package-lock.json
    3: npm install
    4: npm start



OCTOBER 7TH:
    - Posting trail into Journal; plan what is needed (trail ID, user ID)
        - fetch request: `/users/:user_id/journals`                                                     
        - journal controller: #create with params of the dateOfRun and hikerProjectId???
    - GET trails from users' journal and make req to hiker API, while also rendering same info. 
        - Create buttons or check/minus for each item to be packed.